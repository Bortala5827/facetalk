# FaceTalk v2.0 功能说明：30 秒试音互评

> 状态：**已上线**（2026-08-05）。首次访问试音接口时会自动建表（`voice_clips` / `voice_reviews`，录音本体存 R2 对象存储），无需手动去后台操作。当前线上版本为 **v1.0 + v2.0**。

## 一句话定位
匹配成功后、决定组队前，双方先各录一段 **30–60 秒**的试音互听互评；**双方都愿意**才解锁留言板与联机信息，任一方婉拒则房间 **60 秒自动解散**——把"聊了半小时才发现不合适"的精力浪费挡在组队之前。

## 解决的痛点
v1.0 只做文字匹配，双方进了房间才发现语气/表达/默契不对板，浪费时间。v2.0 用一段短录音做"低成本的面对面预筛"。

## 核心流程（强制前置门控）
1. 匹配成功 → 进入 **试音环节**（不解锁留言板/联机信息）。
2. 系统**随机出题**（20 道面试题按房间 ID 确定性哈希，双方同题，零存储）。
3. 各自录 **30–60 秒**试音 → base64 一次上传到 R2（对象存储）。
4. 互听对方录音，从**清晰度 / 逻辑 / 节奏**三维评分，并选择「愿意组队 / 婉拒」。
5. 结果：
   - 双方都「愿意」→ 试音通过，解锁留言板 + 联机信息。
   - 任一方「婉拒」→ 房间进入 **60 秒解散倒计时**，到点自动关闭。

## 关键规则
- **严格时长**：低于 30 秒禁止提交（提示重录），达到 60 秒自动停止；非 30–60 秒的录音服务端直接丢弃。
- **系统随机出题**：题目由 `pairId` 用 FNV-1a 哈希映射到 20 道题之一，双方看到同一题，无需在云端存题。
- **R2 对象存储**：录音在浏览器端用 `MediaRecorder` 采集，转 base64 后一次 POST 上传，服务端解码直写 R2（`clips/{clipId}`）；D1 只存元数据（时长/回听次数/ready），不再占 D1 存储与行配额。
- **内存播放、本地不落盘**：播放用 `URL.createObjectURL(new Blob(...))` 在内存中生成，离开页面即 `revokeObjectURL` 销毁，不写 `localStorage` / `IndexedDB`。
- **阅后即焚**：双方都提交评价后，服务端物理删除全部录音（R2 对象 + D1 元数据行一并清除）；兜底：超过 2 小时无人评价则定时清理强删；R2 生命周期规则（1 天）是最后一道保险，即使定时任务失败录音也会到期自毁。
- **降级兼容**：若 D1 未建 `voice_clips` 等表或 R2 未绑定，`/api/voice` 首次请求会自动建表（D1 绑定有写权限，幂等安全）；若 DDL 被拒或 R2 缺失，则返回 `gate:'skip'`，前端整段跳过试音，老房间与留言板零影响。

## 隐私承诺
录音**仅在会话期间存在于云端缓存**，评价完成或超时后**自动删除**；**云空间与本地设备均不长期储存**任何录音文件。

## 上线条件（已满足，无需手动操作）
- 代码已部署，首次访问试音接口自动建表并激活，无需去后台执行 SQL。
- 手动兜底：若想提前建表，可在 D1 控制台（`mianshi-dazi-d1`）执行 `voice-tables.sql`；R2 bucket `facetalk-voice` 已建好并配 1 天生命周期（`clips/` 前缀自动焚毁）；部署后若报 `error code: 1101`，去 Deployments 点一次 **Retry**。
